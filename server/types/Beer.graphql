type Beer {
  id: ID!
  name: String!
  brewer: Brewer!
}

type RemoveBeerResponse {
  beerId: ID
  success: Boolean!
}

type Mutation {
  removeBeer(brewerId: Int!, beerId: Int!): RemoveBeerResponse!
}